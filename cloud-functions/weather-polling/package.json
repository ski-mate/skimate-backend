{
  "name": "weather-polling",
  "version": "1.0.0",
  "description": "Cloud Function to poll Weather Unlocked API and cache results in Redis",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "gcloud functions deploy pollWeather --gen2 --runtime nodejs20 --trigger-http --allow-unauthenticated --region us-central1"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase-functions": "^4.9.0",
    "redis": "^4.6.13"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
